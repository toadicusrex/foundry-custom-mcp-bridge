# Used by the local Foundry MCP Connector container. Do not commit .env.
# This one token must match the Auth Token in the Foundry Custom MCP Bridge module.
# It also protects the local Codex and CharacterStudio connector calls.
FOUNDRY_MCP_TOKEN=replace-with-the-foundry-module-token
